# Whisky Tree Design QA

## 기준

- 레퍼런스: `whisky-tree.webp`
- 대상: 공개 트리 목록, 단계별 플레이어, 전체 트리, 회원/관리자 편집기
- 주요 뷰포트: PC 편집 화면, PC 사용자 화면, 모바일 읽기 전용 화면

## 구현 점검

- PASS — 레퍼런스의 따뜻한 베이지 캔버스, 흰색 카드, 제품 이미지 중심 노드, 곡선 연결선 구조를 반영했다.
- PASS — 사용자는 현재 노드와 다음 선택지만 먼저 보고, 원할 때 전체 지도를 열 수 있다.
- PASS — 마지막 위스키 노드 도착 시 전체 지도와 선택 경로/현재 위치가 표시된다.
- PASS — PC 편집기에서 좌·우·하단 노드 추가, 노드 삭제, 드래그 이동, 직접 연결/연결 삭제를 지원한다.
- PASS — 노드 이동은 드래그 중 실시간 반영되며, 추가 버튼 방향에 맞는 연결점에서 선이 시작된다.
- PASS — 연결선을 선택해 양 끝을 다른 연결점으로 다시 붙이고 시작·도착 방향을 조절할 수 있다.
- PASS — 관리자 편집기의 노드와 선택 배지는 4~6px 수준의 작은 모서리 반경을 사용한다.
- PASS — 관리자 편집기에서 영어 입력을 숨기고 안내/위스키 노드 설명 및 테스트 미리보기를 제공한다.
- PASS — 노드 위치는 그래프 내부 상태로 드래그 중 갱신하고, 드래그 종료 시 저장 상태에 반영해 깜빡임을 방지한다.
- PASS — 노드의 `+` 버튼 대신 면 연결점을 사용하며, 새 노드는 우측 상단 버튼으로 추가한다.
- PASS — 연결점 드래그 중에는 직선 화살표를 표시하고 저장된 노드 연결선은 기존 곡선 형태를 유지한다.
- PASS — 사이드바에서 업로드 이미지 미리보기, 맞춤/채우기, 확대, 가로·세로 위치, 초기화·삭제를 제공한다.
- PASS — 참고 가격은 `12,000원`처럼 입력·표시되는 단일 텍스트 필드다.
- PASS — 모바일 편집 경로에서는 입력과 저장 버튼을 숨기고 읽기 전용 그래프만 표시한다.
- PASS — 이미지가 없는 상태를 임의 그림이나 가짜 병 이미지로 채우지 않는다.
- PASS — 한국어/영어 UI 키와 술 이름 표시 순서를 분리했다.
- PASS — TypeScript 검사와 Next.js 프로덕션 빌드가 통과했다.

## 시각 비교 상태

- PENDING — 이 세션의 인앱 브라우저가 제공되지 않아 동일 뷰포트 스크린샷을 `whisky-tree.webp`와 나란히 비교하는 자동 시각 QA는 실행하지 못했다.
- 후속 확인: 브라우저 연결이 가능한 세션에서 목록 → 현재 노드 → 전체 지도 → PC 편집기 → 모바일 읽기 전용 순으로 캡처하여 간격, 카드 크기, 이미지 크롭을 최종 확인한다.
