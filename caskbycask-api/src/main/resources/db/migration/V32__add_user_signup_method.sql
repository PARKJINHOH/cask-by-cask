-- =============================================================================
-- CaskByCask — 회원 가입 경로(출처) 구분 컬럼
-- =============================================================================
-- 작성 기준일: 2026-06-14
-- 범위:
--   users.signup_method 추가 — 이메일/네이버/구글 중 어떤 경로로 가입했는지 구분.
--   기존 행(이메일 가입)은 기본값 'EMAIL'. 소셜 신규가입은 OAuthService 가 NAVER/GOOGLE 로 기록.
--   ※ 연동 현황(가입 후 추가 연동 포함)은 user_social_account 가 별도 추적하며,
--     이 컬럼은 "가입 시점 출처" 단일 값으로 변경되지 않는다.
--
-- [주의] Flyway 버전 마이그레이션. 적용 후 수정 금지(체크섬). 보정은 V33__*.sql 로 추가.
-- -----------------------------------------------------------------------------

ALTER TABLE users
    ADD COLUMN signup_method VARCHAR(20) NOT NULL DEFAULT 'EMAIL' COMMENT '가입 경로 — EMAIL/NAVER/GOOGLE';
