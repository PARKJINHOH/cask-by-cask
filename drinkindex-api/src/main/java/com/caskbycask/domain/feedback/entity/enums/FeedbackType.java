package com.caskbycask.domain.feedback.entity.enums;

/**
 * 개선·문의 유형.
 * BUG(기능 버그) / IMPROVEMENT(개선) / FEATURE(기능 추가 요청) / ETC(기타)
 */
public enum FeedbackType {
    BUG,
    IMPROVEMENT,
    FEATURE,
    ETC
}
