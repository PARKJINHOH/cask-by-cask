NEXT_PUBLIC_API_URL=
INTERNAL_API_URL=
# 주류 장소 지도 노출 여부. 서버의 VENUE_ENABLED 와 짝을 이룬다 —
# 서버가 꺼져 있으면 API 가 404 이고, 이 값이 꺼져 있으면 진입점이 숨는다.
NEXT_PUBLIC_VENUE_ENABLED=true
# 지도 스타일(벡터 타일). 비우면 OpenFreeMap 공개 인스턴스를 쓴다.
# 자체 호스팅(Protomaps)으로 옮길 때 이 값만 바꾼다 — mapTiles.ts 주석 참고.
NEXT_PUBLIC_MAP_STYLE_URL=
